# OMNIX

---

OMNIX _nixos configs made easy_. This is a super flexible nixos configuration with
support for custom themes, config overrides, and much more!

**Table of contents**

- INSTALLATION
  - 🚀 ~[quick start]()~
  - ⚙️ [manual configuration]()
- THEMES & OVERRIDES
  - 💾 ~[installing community themes]()~
  - 🎨 ~[create your own theme]()~

**Screenshot**
_none yet sorry :3c_

## INSTALLATION

### Manual configuration

Firstly clone the repository using this command into the directory you want
this config to reside in (I personally leave it at the default `/etc/nixos/`)

```sh
git clone https://github.com/dr4kfire/omnix.git /etc/nixos
```

After that copy the `TEMPLATE` folder from `./hosts` and rename it to the desired
hostname - then
